import React from 'react';
import Header from '../Header';

const Game = () => {
    return (
        <section className='animeLeft'>
            <Header />
            Este é o jogo
        </section>
    );
};

export default Game;
